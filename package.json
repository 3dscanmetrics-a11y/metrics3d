{
  "name": "3dscanmetrics",
  "private": true,
  "description": "3D Scan Metrics public site + MetricsCRM",
  "scripts": {
    "dev:site": "npm run dev --prefix site",
    "dev:crm": "npm run dev --prefix crm",
    "deploy:site": "npm run deploy --prefix site",
    "deploy:crm": "npm run deploy --prefix crm",
    "deploy": "npm run deploy:site && npm run deploy:crm"
  }
}
